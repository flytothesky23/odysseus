# v0.1.7-ko

This patch makes Deep Research management reports fit operations-first Korean business data and keeps wide tables readable in the HTML visual report.

## Highlights

- Reframes `경영분석` as an operations-management report when the evidence is production, sales, logistics, route, byproduct, or field-operation data.
- Keeps corporate finance sections conditional on explicit source evidence.
- Prevents reports from repeatedly generating "자료 없음" rows for finance categories when the source scope is operational.
- Wraps rendered markdown tables in `.table-scroll` so wide tables can scroll horizontally.
- Expands management report layout width on desktop and keeps mobile reports as a single column with horizontal table scroll.

## Verification

- `venv/bin/python -m py_compile src/deep_research.py src/visual_report.py`
- Targeted management category, visual report, and security regression tests.
- `git diff --check`
- Playwright desktop/mobile layout smoke for wide-table horizontal scroll.
- `curl -fsS http://127.0.0.1:7860/api/health`

## Known Gaps

- Full Python test suite and Docker runtime were not rerun for this patch.
- A long live paid-model management report was not regenerated during verification.
